# 버전 관리 정책

> **최초 승인**: 2026-04-14 (D-20260414-01)
> **현재 개정**: 2026-04-18 — ROADMAP v2 (PR #186) EPIC/ISSUE/TASK 체계로 리프레임
> **적용 범위**: 워크스페이스 전 크레이트 (lockstep 버저닝)
> **관련 문서**: [`docs/ROADMAP.md`](../ROADMAP.md) — T1/T2/T3/T4 정의와 릴리스 태그 목록의 canonical source

---

## 1. 규칙 — EPIC/ISSUE 기반

PR #186 의 ROADMAP v2 에서 버전 라인은 Phase-N 가 아닌 EPIC/ISSUE 에 묶입니다. 요약:

| 작업 단위 | 규모 | 버전 영향 | Git tag |
|-----------|------|-----------|---------|
| **EPIC** (T1) | 1-3 개월 | **minor bump** (`0.N.x` → `0.(N+1).0`) | ✅ `vX.Y.0` (EPIC closure commit) |
| **ISSUE** (T2) | 3-10 일 | **patch bump** (`0.N.X` → `0.N.(X+1)`) | ❌ 없음 |
| **TASK / SUBTASK** (T3 / T4) | 반나절 ~ 하루 | 없음 | ❌ 없음 |

- ISSUE 머지 = 단일 PR · 단일 harness green run. 패치 버전만 올라가고 태그는 없음. `Cargo.toml` 의 `[workspace.package] version` 을 머지 커밋에서 bump.
- EPIC closure = 해당 EPIC 의 모든 ISSUE 가 머지된 뒤, "EPIC closure" 커밋에서 minor bump + `vX.Y.0` 태그.
- 0.x 구간 동안 **SemVer 호환성 약속은 없습니다.** 모든 크레이트는 pre-1.0 으로 간주되며, minor bump (=EPIC closure) 에서 breaking change 가 허용됩니다.
- `1.0.0` 은 **진짜 출시 시점** 에만 명시적으로 bump — ROADMAP v2 에서 **EPIC 4 (Multi-tenant / XaaS) closure** 에 연결되어 있습니다.

## 2. 크레이트 버저닝 — Lockstep

모든 워크스페이스 멤버는 `[workspace.package] version` 을 공유합니다 (`version.workspace = true`). 크레이트별 독립 버저닝은 `1.0.0` 이전에는 도입하지 않습니다.

**이유**: 크레이트 경계는 `docs/process/04-decision-log.md` D-12 에서 고정되어 있으나, 내부 타입 흐름이 밀접해 어느 한 크레이트의 breaking change 가 거의 항상 다른 크레이트에 파급됩니다. 독립 버저닝은 운영 이득 없이 릴리스 노트만 복잡하게 만듭니다.

## 3. Patch bump = ISSUE 머지

ISSUE 한 개가 PR 로 머지될 때 항상 patch 를 올립니다. 다음은 최근 머지된 ISSUE 들의 실례 (`docs/ROADMAP.md` §"Completed ISSUEs"):

| 버전 | ISSUE | 머지 PR |
|------|-------|---------|
| `0.2.0` | ISSUE 1 — usable OpenAI-compat gateway + workbench CRUD | #175/#176/#177/#179 (over-split pre-rule) |
| `0.2.1`-`0.2.4` | ISSUE 2 — workbench UX polish + workflow bootstrap | #180/#181/#182/#184 (over-split pre-rule) |
| `0.2.5` | ISSUE 2b — ROADMAP v2 recalibration | #186 |

ISSUE 범위가 너무 커서 하나의 PR 에 안 들어가면 ISSUE 자체를 분할하는 것이 원칙 — "over-split" 되어 여러 PR 로 머지된 ISSUE 1/2 는 ROADMAP v2 에서 사후적으로 묶었고, 이후에는 규칙을 따릅니다.

Phase 내부에서 **breaking change 가 필요하면** 해당 EPIC closure 를 기다리지 말고 별도 결정(decision log)으로 해결 방안을 문서화합니다. 기본은 "다음 EPIC 으로 미룬다".

## 4. Minor bump = EPIC closure

EPIC 의 마지막 ISSUE 가 머지되는 PR 에서 minor bump + git tag 를 진행합니다. 조건:

1. EPIC 에 속한 모든 ISSUE 가 "Completed ISSUEs" 로 이동되고 머지 PR 번호가 기록됨 (`docs/ROADMAP.md` 의 EPIC 섹션 참조).
2. `docs/ROADMAP.md` 의 해당 EPIC 섹션이 "CLOSED" 로 갱신되고 다음 EPIC 이 "ACTIVE" 로 승격됨.
3. `[workspace.package] version` 이 minor bump: 현재 `0.N.X` → `0.(N+1).0`.
4. git tag 를 EPIC closure 커밋에 찍음: `v0.(N+1).0`.
5. 태그 메시지는 해당 EPIC 의 이름 + 주요 deliverable 을 한 줄로 포함합니다.

EPIC 간 breaking change 는 허용됩니다. 각 크레이트의 CHANGELOG (존재 시) 또는 decision log 에 영향받는 공개 API 목록을 명시합니다.

## 5. 태그 네이밍

EPIC closure 태그만 공식 릴리스입니다. 모두 `vX.Y.Z` 형식 (suffix 없음):

| Tag | 조건 | ROADMAP 소스 |
|-----|------|------------|
| `v0.1.0-phase1` | 역사적 Phase 1 스냅샷 (ROADMAP v2 이전) | 기존 유지 |
| `v0.3.0` | EPIC 1 (Workbench MVP) closure | ROADMAP v2 §EPIC 1 |
| `v0.4.0` | EPIC 2 (Agent autonomy) closure | ROADMAP v2 §EPIC 2 |
| `v0.5.0` | EPIC 3 (Plugin platform) closure | ROADMAP v2 §EPIC 3 |
| `v1.0.0` | EPIC 4 (Multi-tenant / XaaS) closure — **first production release** | ROADMAP v2 §EPIC 4 |
| `v2.0.0` | EPIC 5 (Cluster platform) closure | ROADMAP v2 §EPIC 5 |

**Cargo.toml 의 `version` 은 suffix 없이 유지합니다** (crates.io 공개 전이므로 resolver 혼동이 없음). 사전 릴리스 identifier 가 필요한 경우 git tag 에서만 (`v0.3.0-rc.1` 등) 사용합니다.

## 6. CHANGELOG

현재는 `docs/process/04-decision-log.md` + `docs/ROADMAP.md` "Completed ISSUEs" 섹션이 사실상 CHANGELOG 역할을 겸합니다. 별도 `CHANGELOG.md` 는 **`v1.0.0` bump 와 동시에** 도입합니다 (keepachangelog.com 포맷).

그 전까지는:
- 릴리스별 SBOM (`docs/process/00-agent-roster.md`) 는 EPIC closure git tag 단위로 산출.
- 사용자 가시 변경은 decision log + ROADMAP "Completed ISSUEs" 에서 추적.

## 7. 현재 상태

- **Workspace version**: `0.5.9` (EPIC 1 + EPIC 2 + **EPIC 3 all CLOSED**; **EPIC 4 Multi-tenant business ops / XaaS ACTIVE** (promoted 2026-04-20) toward `v1.0.0`; **ISSUE 11 complete** — 4 TASKs across 0.5.1–0.5.4 (PRs #230/#231/#232/#234); **ISSUE 12 closed at telemetry scope** — TASK 12.1 (PR #236 / 0.5.5) + TASK 12.2 (PR #241 / 0.5.6), TASKs 12.3/12.4/12.5 DEFERRED; **ISSUE 14 complete** (PR #246 / 0.5.7) — tenant self-service users + teams + key rotation + CLI; **ISSUE 15 complete** (PR #248 / 0.5.8) — cookie-session login API (`/auth/{login,logout,whoami}`); **ISSUE 16 complete** (PR #259 / 0.5.9) — unified Bearer-or-cookie auth middleware (`auth_middleware` cookie fallback with role-derived scope synthesis; covers `/v1/*` + `/api/v1/web/workbench/*` + `/api/v1/xaas/*`). Remaining: ISSUE 13 HF model catalog discovery/pinning (cost attribution deferred with commercialization), **ISSUE 17** web UI login form (React/Tailwind in `gadgetron-web`))
- **활성 EPIC**: EPIC 1 (Workbench MVP) **CLOSED (v0.3.0)** + EPIC 2 (Agent autonomy) **CLOSED (v0.4.0)** + EPIC 3 (Plugin platform) **CLOSED (v0.5.0, PR #228, 2026-04-20)** + EPIC 4 (Multi-tenant business ops / XaaS) **ACTIVE** (promoted 2026-04-20). EPIC 1 shipped — ISSUE 1 (0.2.0), ISSUE 2 (0.2.1→0.2.4), ISSUE 2b (0.2.5), ISSUE 3 (0.2.6, PR #188), ISSUE 4 (0.2.7, PR #194); EPIC close = minor bump 0.2.12 → 0.3.0 + git tag `v0.3.0` (PR #208). EPIC 2 shipped — ISSUE 5 (0.2.8, PR #199), ISSUE 6 (0.2.9, PR #201), ISSUE 7 (first-class MCP server, 0.2.9 → 0.2.12, 3 PRs #204/#205/#207 — discovery + invoke + cross-session audit); EPIC close = minor bump 0.3.0 → 0.4.0 + git tag `v0.4.0` (PR #209). EPIC 3 shipped (CLOSED v0.5.0) — **ISSUE 8 (DescriptorCatalog hot-reload)** functionally complete: TASK 8.1 shipped (0.4.0 → 0.4.1, PR #211 — `Arc<ArcSwap<DescriptorCatalog>>` plumbing substrate); **TASK 8.2 shipped (0.4.1 → 0.4.2, PR #213 — `POST /api/v1/web/workbench/admin/reload-catalog` Management-scoped endpoint; atomic `store()` on the ArcSwap; in-flight invocations finish against their snapshot)**; **TASK 8.3 shipped (0.4.2 → 0.4.3, PR #214 — `CatalogSnapshot { catalog, validators }` bundling; handle widened from `Arc<ArcSwap<DescriptorCatalog>>` to `Arc<ArcSwap<CatalogSnapshot>>` so a reload swaps catalog + pre-compiled JSON-schema validators in lockstep, closing the TASK 8.2 known-limitation where validators were pre-compiled and not rebuilt on reload)**; **TASK 8.4 shipped (0.4.3 → 0.4.4, PR #216 — file-based catalog source; new `[web] catalog_path` config key, `DescriptorCatalog::from_toml_file()` parser, reload handler parses + swaps atomically (parse failure → 500 without replacing running snapshot), response shape widens with `source: "config_file"` + `source_path: "/path/to/file"` when configured)**; **TASK 8.5 shipped (0.4.4 → 0.4.5, PR #217 — POSIX `SIGHUP` reloader; `spawn_sighup_reloader()` Unix-only tokio task listens on `SignalKind::hangup()` and invokes the shared `perform_catalog_reload()` helper — same `ReloadCatalogResponse` + tracing telemetry as the HTTP endpoint. Operator workflow: edit `catalog_path`, `kill -HUP <pid>`. fs-watcher deferred to future TASK 8.6 if demand materializes — SIGHUP covers the 90% case with no extra deps or background threads under steady state)**. **ISSUE 9 (real bundle manifests) shipped** — **TASK 9.1 shipped (0.4.5 → 0.4.6, PR #219 — `BundleMetadata { id, version }` struct attaches to `DescriptorCatalog` via optional `[bundle]` TOML table; reload response widens with `bundle: Option<BundleMetadata>`, omitted via `skip_serializing_if` when `None`; first-party bundle file at `bundles/gadgetron-core/bundle.toml` that mirrors `seed_p2b()` exactly, guarded by a drift test that asserts both sources produce the same action id set; TOML shape = TASK 8.4 `CatalogFile` + optional `[bundle]` table at top, backwards-compatible)**; **TASK 9.2 shipped (0.4.6 → 0.4.7, PR #220 — multi-bundle aggregation; new `WebConfig.bundles_dir: Option<String>` config key + `DescriptorCatalog::from_bundle_dir()` scans every `<dir>/<subdir>/bundle.toml` and merges views + actions into one catalog in deterministic alphabetical path order for idempotent reloads; duplicate action OR view ids across bundles surface as a hard `Config` error naming the id + both contributing bundle ids, running snapshot NOT replaced on failure; `allow_direct_actions` OR-folded across bundles; reload response gains `bundles: Vec<BundleMetadata>` via `skip_serializing_if = "Vec::is_empty"`; precedence `bundles_dir` > `catalog_path` > `seed_p2b` fallback; subdirectories without `bundle.toml` silently skipped; 3 new unit tests cover merge + duplicate-id error + missing-directory path)**; **TASK 9.3 shipped (0.4.7 → 0.4.8, PR #222 — bundle-driven harness default; E2E harness config `scripts/e2e-harness/fixtures/gadgetron-test.toml.tmpl` now carries `[web] bundles_dir = "@BUNDLES_DIR@"` resolved to the repo-root `bundles/` path by `run.sh`, so every harness run exercises the `from_bundle_dir` path instead of `seed_p2b()` fallback; Gate 7q.1 now asserts `source == "bundles_dir"` (was `"seed_p2b"`); new Gate 7q.3 pins `.bundles[0].id == "gadgetron-core"` so a rename of the first-party bundle or a silent drop of the contributing-bundles list trips the gate on the next run; `seed_p2b()` stays alive as a unit-test fixture and drift-guard reference — production code still falls back to it at the handler level when neither `bundles_dir` nor `catalog_path` is configured, but the shipped harness + docs surface the bundle path as the canonical default; closes ISSUE 9 — all 3 TASKs landed; harness PASS 83 → 84 via Gate 7q.3)**. **ISSUE 10 (bundle marketplace) shipped** — **TASK 10.1 shipped (0.4.8 → 0.4.9, PR #223 — `GET /api/v1/web/workbench/admin/bundles` Management-scoped bundle-discovery endpoint; read-only scan of `bundles_dir` subdirectories returning `{bundles_dir, count, bundles: [{bundle?, source_path, action_count, view_count}, ...]}` in deterministic subdir-sorted order matching `from_bundle_dir`; does NOT ArcSwap the live catalog so polling is safe while requests in flight; 503 `Config` when `bundles_dir` isn't wired; new Gate 7q.4 pins shape + `gadgetron-core` enumeration with `action_count == 5` + new Gate 7q.5 pins OpenAiCompat → 403 RBAC; harness PASS 84 → 86)**; **TASK 10.2 shipped (0.4.9 → 0.4.10, PR #224 — install + uninstall runtime; `POST /api/v1/web/workbench/admin/bundles` accepts `{bundle_toml: "<text>"}`, parses + validates (manifest must declare `[bundle]` with id matching `^[a-zA-Z0-9_-]{1,64}$` for path-traversal safety via `validate_bundle_id()` helper), writes manifest verbatim to `{bundles_dir}/{id}/bundle.toml`, returns `{installed, bundle_id, manifest_path, reload_hint}`; `DELETE /api/v1/web/workbench/admin/bundles/{bundle_id}` removes the directory via `std::fs::remove_dir_all` with same id regex guard; both Management-scoped; composable with reload — install writes to disk but does NOT swap live catalog (operator must trigger `POST /admin/reload-catalog` or SIGHUP to activate, `reload_hint` field in response explicitly points at the reload endpoint); collision policy: installing over existing id → 4xx `Config` (matches `from_bundle_dir` duplicate-id hard-error); 3 new Gates — 7q.6 (install → discovery lists it), 7q.7 (path-traversal `id = "../etc/passwd"` → 4xx rejection), 7q.8 (uninstall → discovery no longer lists it); harness PASS 86 → 91)**; **TASK 10.3 shipped (0.4.10 → 0.4.11, PR #226 — per-bundle scope isolation; `BundleMetadata.required_scope: Option<Scope>` new serde-optional TOML field at the `[bundle]` level; `DescriptorCatalog::from_toml_file` post-parse pass walks every view + action and inherits `bundle.required_scope` onto descriptors without their own — narrower explicit scopes keep theirs; zero-overhead for bundles that don't declare a scope (default `None`, no inheritance pass); `from_bundle_dir` inherits same behavior via per-manifest `from_toml_file` call; unit test pins both halves of visibility check (OpenAiCompat actor does NOT see Management-floored action; Management actor does) + asserts effective scope lands on the descriptor for downstream audit/log introspection; no new harness gates — existing gates continue covering reload + discovery invariants regardless of scope inheritance; harness PASS stays at 91)**; **TASK 10.4 shipped (0.4.11 → 0.4.12, PR #227 — Ed25519 signed manifests; closes ISSUE 10 and covers the supply-chain attack surface opened by TASK 10.2 letting HTTP write arbitrary TOML to disk; `[web.bundle_signing] { public_keys_hex: Vec<String>, require_signature: bool }` new config block in gadgetron-core with defaults that preserve TASK 10.2 unsigned behavior; `InstallBundleRequest` widens with `signature_hex: Option<String>`; `verify_bundle_signature` runs BEFORE `toml::from_str` so signed-malformed takes same error path as unsigned-malformed — no signer leak via error text; 6-branch policy matrix (unsigned-allowed / unsigned-required / sig-valid / sig-tampered / sig-unknown-key / signature-without-trust-anchors) each pinned by a unit test; workspace picks up `ed25519-dalek = "2"` + `hex = "0.4"` deps; no new harness gates — harness stays 91 PASS; closing rationale: fail loud on signature-without-trust-anchors rather than silently accept, since a misconfigured deployment would otherwise trust any signer)**. **EPIC 3 CLOSED — ISSUE 8 + ISSUE 9 + ISSUE 10 all landed. Closing PR #228 bumped `0.4.12` → `0.5.0` + git tag `v0.5.0` on 2026-04-20.** **EPIC 4 (Multi-tenant business ops / XaaS) now ACTIVE** (promoted 2026-04-20) toward `v1.0.0` (first production release). **ISSUE 11 (quotas + rate limits) complete** — all 4 TASKs shipped across 0.5.1 / 0.5.2 / 0.5.3 / 0.5.4 (PRs #230 / #231 / #232 / #234). **TASK 11.1 shipped (0.5.0 → 0.5.1, PR #230 — structured 429 + `Retry-After` header; every `ApiError` response with HTTP 429 now carries both the `Retry-After: 60` HTTP header AND a `retry_after_seconds: 60` field in the JSON body; SDK clients that honor either surface can back off deterministically instead of retrying in a tight loop; `QUOTA_RETRY_AFTER_SECONDS = 60` is a conservative constant today — TASK 11.2 will thread the real token-bucket refill time through `QuotaToken` so the header reports the exact countdown instead of a safe upper bound; non-429 paths (403, 401, 404) keep the base body shape since `Retry-After` has specific semantics clients act on; 2 new unit tests in `error::tests`, no new harness gates because in-memory enforcer never triggers 429 on harness traffic)**; **TASK 11.2 shipped (0.5.1 → 0.5.2, PR #231 — per-tenant token-bucket rate limiter; new `TokenBucketRateLimiter` in `gadgetron-xaas::quota::rate_limit` with DashMap-sharded per-tenant buckets, lazy refill at `consume()` time, monotonic `Instant` clock (wall-clock skew can't double-spend), fractional tokens so tiny refills don't round to zero; `RateLimitedQuotaEnforcer` composite wraps `Arc<dyn QuotaEnforcer>` + limiter, `check_pre` runs rate check FIRST (fail fast without snapshot query on rejection) then delegates to inner cost enforcer; opt-in via new `[quota_rate_limit] requests_per_minute = N, burst = M` config block — default `requests_per_minute = 0` makes limiter a no-op (preserves TASK 11.1 / pre-PR-#231 behavior so existing deployments see zero change); `burst = 0` defaults to `requests_per_minute` so fresh tenants don't get surprise bursts they can't sustain; CLI wires the wrapper at `init_serve_runtime` only when enabled; 5 new unit tests pin bucket semantics (within-burst accepts, exceeds-burst rejects, refill-after-wait restores, disabled always accepts, per-tenant independence); no new harness gates because harness leaves `quota_rate_limit` disabled — unit tests carry the correctness proof)**; **TASK 11.3 shipped (0.5.2 → 0.5.3, PR #232 — Postgres-backed daily + monthly spend tracking; new `PgQuotaEnforcer` in `gadgetron-xaas::quota::enforcer` with `record_post` that runs one UPDATE against `quota_configs` incrementing `daily_used_cents` + `monthly_used_cents` with CASE-expression rollover (zeros daily at UTC midnight, monthly at first-of-month — no background job, rollover happens on the first post-boundary request); new migration `20260420000001_quota_usage_day.sql` adds `usage_day DATE DEFAULT CURRENT_DATE` column to `quota_configs` (default today for existing rows so first post-migration request doesn't spuriously zero monthly usage, partial index for operator spend reports); CLI `init_serve_runtime` picks `PgQuotaEnforcer` when `pg_pool.is_some()` else falls back to `InMemoryQuotaEnforcer`; both wrapped by `RateLimitedQuotaEnforcer` when `[quota_rate_limit]` is configured (TASK 11.2 composite); `check_pre` still delegates to the middleware-pre-fetched `QuotaSnapshot` (future follow-up could add a per-request fresh query to tighten race windows); `record_post` is fire-and-forget — UPDATE errors log as `tracing::error` but never propagate (request already succeeded); pipeline order = rate-limit check → pg cost check → dispatch → pg record_post; harness covers via bootstrap populating `quota_configs` so `record_post` fires on every harness chat completion, e2e coverage proves UPDATE semantics)** + **TASK 11.4 shipped (0.5.3 → 0.5.4, PR #234 — `GET /api/v1/web/workbench/quota/status` tenant introspection endpoint (OpenAiCompat scope — users check their own usage without Management rights); response shape `{usage_day, daily: {used_cents, limit_cents, remaining_cents}, monthly: {...}}` all in cents; SQL does the CASE rollover inline so response already reflects boundary crossings without the handler doing the math; bootstrap-gap fallback — when `quota_configs` has no row for the tenant, handler returns schema defaults (1_000_000 cents daily, 10_000_000 cents monthly, usage_day = today) so UI renders "fresh tenant, full quota" instead of 400 during tenant provisioning; 503 `config_error` when `pg_pool` is not wired; new harness gate 7k.5 pins shape + fallback — harness test config doesn't populate `quota_configs` so the gate specifically exercises the fallback path; closes ISSUE 11 — all 4 TASKs landed across 0.5.1/0.5.2/0.5.3/0.5.4)**. Full EPIC 4 quota pipeline now: rate-limit check → pg cost check → dispatch → pg record_post → tenant introspection via /quota/status → rejections surface as structured 429 + Retry-After. **ISSUE 12 (integer-cent billing) closed at telemetry scope (PR #241 / 0.5.6)** — **TASK 12.1 shipped (0.5.4 → 0.5.5, PR #236 — append-only `billing_events` ledger; new `gadgetron-xaas::billing::events` module; new migration `20260420000002_billing_events.sql` creates table with integer cents, `event_kind ∈ {chat, tool, action}` CHECK constraint, FK-less `source_event_id` per Stripe-style writer-independence pattern, `(tenant_id, created_at DESC)` + `(event_kind, created_at DESC)` indexes; `PgQuotaEnforcer::record_post` now fires one `insert_billing_event` INSERT per chat completion alongside the existing `quota_configs` counter UPDATE — ledger failures log as `tracing::warn!(target: "billing")` but don't propagate, counter-ahead-of-ledger reconciliation is TASK 12.4; new `GET /api/v1/web/workbench/admin/billing/events` Management-scoped ledger query (newest-first, `limit.clamp(1, 500)`, optional `since` lower bound, handler pins tenant boundary so cross-tenant reads are impossible regardless of query params — OpenAiCompat → 403 via `scope_guard_middleware`); new harness gate 7k.6 pins shape + tenant-boundary behavior; `docs/design/xaas/phase2-billing.md` new design doc carries STRIDE delta + SQL + trait signatures matching landed code)**. **TASK 12.2 shipped (0.5.5 → 0.5.6, PR #241 — tool + action billing emission; `/v1/tools/{name}/invoke` success path and workbench direct + approved action success paths each emit one `billing_events` row (kind=tool / kind=action). Action rows carry `source_event_id = audit_event_id` for clean audit↔ledger joins. Fire-and-forget `tokio::spawn`, matches audit-sink non-blocking contract. Harness gates 7i.5 + 7h.1c. Closes ISSUE 12 at telemetry scope)**. Remaining ISSUE 12 TASKs (12.3 invoice materialization views, 12.4 counter/ledger reconciliation, 12.5 Stripe ingest) + ISSUE 13 (HuggingFace model catalog) + ISSUE 14 (tenant self-service) are **commercialization layer, DEFERRED per 2026-04-20 scope direction** — they do NOT gate the `v1.0.0` release cut. EPIC 4 "close for 1.0" = ISSUE 11 + core product surfaces (knowledge + Penny + bundle/plug + multi-user + observability) meeting production bar. 12.3 / 12.4 / 12.5 / 13 / 14 TASKs ship post-1.0 as patch / minor bumps once market pull justifies. EPIC close → `0.5.x` → `v1.0.0` (major bump because API stabilizes).
- **다음 minor bump**: EPIC 4 (Multi-tenant XaaS) close 시 `v1.0.0` (first production release, major bump because API stabilizes). EPIC 5 (Cluster platform) close 시 `v2.0.0` (post-1.0). 0.5.x patch bumps will accumulate per-ISSUE within EPIC 4 in the meantime.
- **이전 tag**: `v0.5.0` (EPIC 3 closure, 2026-04-20), `v0.4.0` (EPIC 2 closure, 2026-04-19), `v0.3.0` (EPIC 1 closure, 2026-04-19), `v0.1.0-phase1` (역사적).
